---
import BaseLayout from '../layouts/BaseLayout.astro';

import ContactCTA from '../components/ContactCTA.astro';
import Hero from '../components/Hero.astro';
---

<BaseLayout title="Acerca de | Servicons" description="Somos construcción, obra blanca y acabados.">
	<div class="stack gap-20">
		<main class="wrapper about">
			<Hero
				title="Acerca de"
				tagline="Servicons - Construcción, obra blanca y acabados."
			>
				<img
					width="1553"
					height="873"
					src="/assets/at-work.jpg"
					alt="Servicons - Construcción, obra blanca y acabados."
				/>
			</Hero>

			<section>
				<h2 class="section-title">Propósito</h2>
				<div class="content">
					<p>
						En Cali, Servicons es reconocida por su excelente servicio, ofreciendo soluciones de construcción, obra blanca y acabados eficientes; además de reparaciones.
					</p>
					<p>
						Con profesionales altamente capacitados con años de experiencia en el campo, con excelentes resultados. Dinos si necesitas encargar un constructor o técnico en Cali.
					</p>
				</div>
			</section>
			<!-- <section>
				<h2 class="section-title">Education</h2>
				<div class="content">
					<p>Corporis voluptates tenetur laudantium.</p>
				</div>
			</section>
			<section>
				<h2 class="section-title">Skills</h2>
				<div class="content">
					<p>officia unde omnis</p>
				</div>
			</section> -->
		</main>

		<ContactCTA />
	</div>
</BaseLayout>

<style>
	.about {
		display: flex;
		flex-direction: column;
		gap: 3.5rem;
	}

	img {
		margin-top: 1.5rem;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	section {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		color: var(--gray-200);
	}

	.section-title {
		grid-column-start: 1;
		font-size: var(--text-xl);
		color: var(--gray-0);
	}

	.content {
		grid-column: 2 / 4;
	}

	.content :global(a) {
		text-decoration: 1px solid underline transparent;
		text-underline-offset: 0.25em;
		transition: text-decoration-color var(--theme-transition);
	}

	.content :global(a:hover),
	.content :global(a:focus) {
		text-decoration-color: currentColor;
	}

	@media (min-width: 50em) {
		.about {
			display: grid;
			grid-template-columns: 1fr 60% 1fr;
		}

		.about > :global(:first-child) {
			grid-column-start: 2;
		}

		section {
			display: contents;
			font-size: var(--text-lg);
		}
	}
</style>
